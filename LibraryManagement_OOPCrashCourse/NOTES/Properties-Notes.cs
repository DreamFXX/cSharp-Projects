namespace LibraryManagementSystem_OOP.NOTES;

internal class Properties_Notes
{
    // Showcase of the operations that Properties with getter n setters do.
    /* internal class Book
    {
        private string name;
        private int pages;

        public string GetName()
        {
            return name;
        }

        public void SetName(string value)
        {
            name = value;
        }

        public int GetPages()
        {
            return pages;
        }

        public void SetPages(int value)
        {
            pages = value;
        } */

    /* Getters and setters are methods associated with properties. 
     * A getter is a method that retrieves the value of a property, while a setter is a method that sets-
     * -or updates the value of a property.
     * This allows you to add logic during the retrieval or assignment of values, such as validation or logging,
     * without exposing the underlying field directly. */

    //internal class CSharp_Properties // Properties looks like this in csharp.
    //{
    //    public string Name { get; set; }
    //    public int Pages { get; set; }
    //}

    //internal class AutoProperties // Setting default name and page count
    //{
    //    public string Name { get; set; } = "Default Name";
    //    public int Pages { get; set; } = 100;
    //}
}

